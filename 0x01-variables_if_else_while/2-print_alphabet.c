#include <stdio.h>

/**
 * main - prints alpha in lower case
 * Return: Always 0
 *
 */

int main(void)
{
	char t;
	
	for (t = 'a'; t <= 'z'; t++)
	{
	     putchar(t);
	}
	putchar('\n');
        return (0);
}
