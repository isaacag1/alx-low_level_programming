#include <stdio.h>
#include <unistd.h>
/**
 * main - prints alpha in lower case
 * Return: Always 0
 *
 */
int main(void)
{
	int ch;	
	
	for (lc = 'a'; lc <= 'z'; lc++);
	{
	     putchar(ch);
	}
	putchar('\n');
return (0);
}
